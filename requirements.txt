# Kern-Web-Admin (läuft überall)
Flask>=3.0,<4
Flask-WTF>=1.2
Flask-Limiter>=3.5
Werkzeug>=3.0
itsdangerous>=2.2
argon2-cffi>=23.1
requests>=2.31
fpdf2>=2.7
waitress>=3.0
python-dotenv>=1.0
qrcode[pil]>=7.4
Pillow>=10.0

# Zeitzonen unter Windows/Alpine (kein System-IANA verfügbar)
tzdata>=2024.1; sys_platform == "win32"

# GUI + Hardware (nur auf dem Raspberry Pi installieren via requirements-pi.txt)
